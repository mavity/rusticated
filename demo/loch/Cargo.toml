[package]
name = "loch"
version = "0.1.0"
edition = "2024"

[dependencies]
rusticated = { path = "../../", default-features = false, features = ["std"] }
ratatui = { version = "0.29.0", default-features = false }
