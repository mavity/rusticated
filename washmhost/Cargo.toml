[package]
name = "washmhost"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmtime = { version = "45", default-features = false, features = ["runtime", "pulley"] }
anyhow = "1"


